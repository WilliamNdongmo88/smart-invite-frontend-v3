import { Component } from '@angular/core';
@Component({ selector: 'app-event-create', standalone: true, template: `<p>Event Create — à implémenter (Phase 3)</p>` })
export class EventCreateComponent {}
