import { Component } from '@angular/core';
@Component({ selector: 'app-event-detail', standalone: true, template: `<p>Event Detail — à implémenter (Phase 3)</p>` })
export class EventDetailComponent {}
